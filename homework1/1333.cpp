#include<cstdio>
#include<cstring>
#include<cstdlib>
#include<iostream>
#include<algorithm>
using namespace std;

const int N = 100010;

int n, h, a[N];
int main() {
	scanf("%d%d", &n, &h);
	for(int i = 1; i <= n; i ++) scanf("%d", &a[i]);
	return 0;
}
